[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  VITE_MOCK_MODE = "true"

[context.deploy-preview.environment]
  VITE_MOCK_MODE = "true"
